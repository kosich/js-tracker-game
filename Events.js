(function(){ 
    'use strict';
    var E = helper.defineNS('Events');
    E.Move = {
        attarget : 'move.atTargetPoint'
    };

    //Sounds events domain
    E.Sound = {
        Basic : 'sound.basic'
    };
    E.Action = {
        Hit : 'action.hit'
    };
 
})();

